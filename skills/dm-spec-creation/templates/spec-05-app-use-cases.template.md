# Specification: Application Use Cases

> Application-layer orchestrators. Coordinate; do not redefine domain rules.

## 1. Use Case Inventory

<!-- table: name | trigger source | inbound boundary | owning module -->

## 2. Per-Use-Case Specifications

<!-- per use case template:

### 2.N <Verb the Object>
- Trigger(s):
- Inbound boundary:
- Input shape (spec-01 terms):
- Preconditions:
- Steps (calls to domain collaborators):
- Postconditions (spec-01 pinks created / transitioned):
- Output shape:
- Error outcomes (each named; mapped to spec-03 error class + spec-04 feedback location):
- Authorization rule:
- Idempotency posture:
- Concurrency posture:
- Observability (logs, metrics):
- Contract shape (only if API-exposed; field-level, follows spec-03 naming):
-->

## 3. Coverage Cross-References

### 3.1 Acceptance Criteria → Use Cases

### 3.2 UI Actions → Use Cases

### 3.3 Pink Lifecycle Transitions → Use Cases
